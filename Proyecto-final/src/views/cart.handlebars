<div>
  <table>
    <tr>
    <th>Carrito</th>
    <th>Id del Producto</th>
    <th>Cantidad</th>
    
  </tr>
{{#each cart}}
      {{#each this.products}}
        <tr>
          <td>{{../_id}}</td>
            <td>{{this.product}}</td> <!-- Utilizar notación de corchetes para acceder a la propiedad -->
          <td>{{this.quantity}}</td>
        </tr>
      {{/each}}
    {{/each }}
  </table>  


</div>